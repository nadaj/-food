# https://travis-ci.com/nadaj/-food
language: python
cache:
  directories:
    - "$HOME/.cache/pip"
python:
  - "3.6.4"
  - "nightly"
branches:
  only:
    - master
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
# command to run tests
script: pytest